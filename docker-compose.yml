services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: sportquest
      POSTGRES_PASSWORD: sportquest
      POSTGRES_DB: sportquest
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
